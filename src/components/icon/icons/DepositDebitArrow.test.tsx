import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import DepositDebitArrow from './DepositDebitArrow';
test('DepositDebitArrow component should render correctly', () => {
  render(<DepositDebitArrow />);
});
