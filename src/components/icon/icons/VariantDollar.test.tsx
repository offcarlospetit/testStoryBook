import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import VariantDollar from './VariantDollar';

test('VariantDollar component should render correctly', () => {
  render(<VariantDollar />);
});
