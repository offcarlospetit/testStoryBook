import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import TCGold from './TC-gold';
test('TCGold component should render correctly', () => {
  render(<TCGold />);
});
