import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import PatAutoExcluyente from './PatAutoExcluyente';
test('PatAutoExcluyente component should render correctly', () => {
  render(<PatAutoExcluyente />);
});
