import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import MovementDebitArrowCharge from './MovementDebitArrowCharge';
test('Movement Debit Arrow Charge component should render correctly', () => {
  render(<MovementDebitArrowCharge />);
});
