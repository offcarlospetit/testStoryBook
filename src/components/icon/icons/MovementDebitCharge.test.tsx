import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import MovementDebitCharge from './MovementDebitCharge';
test('Movement Debit Charge component should render correctly', () => {
  render(<MovementDebitCharge />);
});
