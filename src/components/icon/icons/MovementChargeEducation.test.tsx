import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import MovementChargeEducation from './MovementChargeEducation';
test('Movement Charge Education component should render correctly', () => {
  render(<MovementChargeEducation />);
});
