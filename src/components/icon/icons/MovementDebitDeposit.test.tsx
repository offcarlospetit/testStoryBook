import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import MovementDebitDeposit from './MovementDebitDeposit';
test('Movement Debit Deposit component should render correctly', () => {
  render(<MovementDebitDeposit />);
});
