import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import MovementDebitArrowDeposit from './MovementDebitArrowDeposit';
test('Movement Debit Arrow Deposit component should render correctly', () => {
  render(<MovementDebitArrowDeposit />);
});
