import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import Distribucion from './Distribucion';
test('Distribucion component should render correctly', () => {
  render(<Distribucion />);
});
