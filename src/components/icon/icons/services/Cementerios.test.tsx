import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import Cementerios from './Cementerios';
test('Cementerios component should render correctly', () => {
  render(<Cementerios />);
});
