import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import Arriendo from './Arriendo';
test('Arriendo component should render correctly', () => {
  render(<Arriendo />);
});
