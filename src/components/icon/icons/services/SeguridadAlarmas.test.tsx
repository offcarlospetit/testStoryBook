import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import SeguridadAlarmas from './SeguridadAlarmas';
test('SeguridadAlarmas component should render correctly', () => {
  render(<SeguridadAlarmas />);
});
