import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import Mutuales from './Mutuales';
test('Mutuales component should render correctly', () => {
  render(<Mutuales />);
});
