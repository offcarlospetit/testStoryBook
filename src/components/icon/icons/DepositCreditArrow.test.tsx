import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import DepositCreditArrow from './DepositCreditArrow';
test('DepositCreditArrow component should render correctly', () => {
  render(<DepositCreditArrow />);
});
