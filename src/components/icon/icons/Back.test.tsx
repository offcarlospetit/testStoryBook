import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import Back from './Back';
test('Back component should render correctly', () => {
  render(<Back />);
});
