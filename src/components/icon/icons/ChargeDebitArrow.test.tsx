import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import ChargeDebitArrow from './ChargeDebitArrow';
test('ChargeDebitArrow component should render correctly', () => {
  render(<ChargeDebitArrow />);
});
