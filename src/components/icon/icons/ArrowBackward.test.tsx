import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import ArrowBackward from './ArrowBackward';
test('Charge component should render correctly', () => {
  render(<ArrowBackward />);
});
