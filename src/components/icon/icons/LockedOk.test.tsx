import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import LockedOk from './LockedOk';
test('LockedOk component should render correctly', () => {
  render(<LockedOk />);
});
