import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import MenuActive from './MenuActive';
test('MenuActive component should render correctly', () => {
  render(<MenuActive />);
});
