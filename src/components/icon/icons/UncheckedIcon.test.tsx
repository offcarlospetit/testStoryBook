import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import UncheckedIcon from './UncheckedIcon';
test('UncheckedIcon component should render correctly', () => {
  render(<UncheckedIcon />);
});
