import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import AlertFense from './AlertFense';
test('AlertFense component should render correctly', () => {
  render(<AlertFense />);
});
