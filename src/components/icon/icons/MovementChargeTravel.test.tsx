import { render } from '@testing-library/react-native';
import React from 'react';
import 'react-native';
import MovementChargeTravel from './MovementChargeTravel';
test('Movement Charge Travel component should render correctly', () => {
  render(<MovementChargeTravel />);
});
